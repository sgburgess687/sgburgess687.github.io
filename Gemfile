source "https://rubygems.org"

# Pins Jekyll + plugins to exactly what GitHub Pages builds with,
# so local `bundle exec jekyll serve` matches the live site.
gem "github-pages", group: :jekyll_plugins

gem "webrick"
